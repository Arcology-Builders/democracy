import React from "react";

export const Offline = () => (
  <div>
      Hello, it seems like you are Offline! Please check your internet connectivity, 
      then try reloading the page.
  </div>
);

export default Offline;
