const { constructCxPipeline } = require('..')

describe('Confidential transfer pipeline', () => {
  
  it('constructs the pipeline', async () => {
    const cxPipeline = constructCxPipeline() 
  })

})

